# @kids-chatbot/story-types

Core TypeScript interfaces that describe sessions, graph nodes, and responses shared across the Node-based chatbot stack.

> Implementation incoming: this package currently exposes placeholder exports so downstream packages can wire up build tooling before business logic is ported.
